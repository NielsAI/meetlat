__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Run output: reports are reproducible from a checkpoint plus a prompt seed, so
# they are artifacts, not sources. Judge cards and gold sets are sources and are
# deliberately NOT ignored.
runs/
*.local.json
.env

# Phase plan
docs/agent-phases.md
